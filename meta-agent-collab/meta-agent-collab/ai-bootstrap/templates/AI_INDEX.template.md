<!--
  ============================================================
  AI_INDEX.template.md · 注释版样板
  ============================================================
  用途：02-build-order 用本模板建 AI_INDEX.md

  本文件的灵魂：告诉新 AI 三层路由——会话启动读什么、每条消息查什么、关键动作前扫什么。
  关键约束：**只列入口与顺序，不复述被指向文件的内容**。

  AI 改写流程：
    1. 通读
    2. **按读者实际建了哪些文件删减**——没建的文件不能列！
       例：读者没建 PROFILE → 「新会话启动」不列；没建 AI_SELF_CHECK → 「动作门前」不列
    3. 删掉所有 <!-- AI注: --> 注释
    4. 50 行内
  ============================================================
-->

# AI 接手索引

> **本文是什么**：AI 在本工作区协作时的文件路由表。分四层——会话启动、每条消息、每次回合结束、动作门前。
> **本文不说**：任何具体规则的内容（去对应文件读原文，避免双地点同步）。
> **行数预算**：50 行内；超出请拆，不追加。

---

## 新会话启动（一个会话窗口开始时 · 一次性 · 约 5 分钟）

按顺序读，不要乱序：

<!-- AI注: 按读者实际建的文件删减 -->
[如建了 heartbeat] 0. `<工作区根>/heartbeat-state.md` — 上轮是否正常收尾（CodeBuddy 规则强制加载）
1. `<工作区根>/AGENT.md` — 跨项目通用协作姿态、全部 HOW 规则（必有）
[如建了 memory] 2. `<工作区根>/memory.md` — 活跃工作集（近期模式、待确认偏好）
[如建了 PROFILE] 3. `<工作区根>/PRODUCER_PROFILE.md` 顶部「启动必读 · TL;DR」— 制作人速览（正文按需深入）
4. `<工作区根>/NOW.md` — 当前在哪个项目（必有）
[如建了项目 README] 5. `<当前项目>/README.md` — 项目总览
[如建了项目 NOW] 6. `<当前项目>/NOW.md` — 当前项目进度
[如建了项目 AGENT] 7. `<当前项目>/AGENT.md` — 项目代码/设计契约
[如建了 CHANGELOG] 8. `<当前项目>/CHANGELOG.md` — 近期变更

> 读完这些（0~4 为必读，5~8 按当前项目），你应该能回答："今天该做什么？该按哪些规则做？"
> 答不上来 = 没读完，回去补；不要凭印象开工。

---

## 每次消息（收到用户每条消息时 · 轻量 · 约 3 秒）

<!-- AI注: heartbeat 组合 B+ 才建，组合 A/C 不建则跳过本段 -->
[如建了 heartbeat] 1. `<工作区根>/heartbeat-state.md` — 上轮是否有未 commit 改动 / 收尾未完成（~16 行）

> 读完决定：上轮尾巴没收 → 先补收尾再回应新消息；已收 → 继续。

---

## AI 输出结束时（每轮回复完成后 · 轻量 · 约 3 秒）

<!-- AI注: heartbeat 组合 B+ 才建，组合 A/C 不建则跳过本段 -->
[如建了 heartbeat] 1. `<工作区根>/heartbeat-state.md` — 由 heartbeat 自主判断是否需要执行收尾（见该文件顶部「输出结束时·自主判断」段）
2. **确认无修改** — 检查本轮是否有未 commit 的文件改动。若有 → 先执行收尾 + commit

> heartbeat 自己承载判断逻辑——不依赖 AI 记住 AGENT §14。本轮有改动 + 收尾未跑 → heartbeat 要求立即执行收尾。

---

## 动作门前（准备"下结论 / 动手破坏性操作 / 宣告完成 / 执行收尾"前扫）

<!-- AI注: AI_SELF_CHECK 组合 E 才建，其他组合不建则跳过本段 -->
[如建了 AI_SELF_CHECK] 1. `<工作区根>/AI_SELF_CHECK.md` — 违反信号速查表。扫一遍确认没有正在违反的 HOW 规则，命中 → 先自救再继续
2. **收尾检查**（准备 commit 前强制）— 本轮有文件改动但收尾未执行？→ 先跑收尾三件套，最后一步 commit

---

## 按需深入（不要默认全读）

<!-- AI注: 按读者实际建的删减 -->
[如建了 PROFILE] - `PRODUCER_PROFILE.md` 正文 — 周度 review / 更新 PROFILE 时
[如建了] - `<当前项目>/MAJOR_BUGS.md` — 触发"重大 Bug 排查"流程时
- `<当前项目>/docs/` — 涉及具体设计改动时
[如建了 reviews] - `reviews/` — 周度复盘时

## 默认不读（即使搜索命中也跳过）

> 这些文件存在但不参与"当前契约"的判断，读了反而引入漂移。

<!-- AI注: 按读者实际有什么填 -->
[如读者复制了 meta-agent-collab] - `meta-agent-collab/` — 对外教学品，不是协作契约
- 任何带"已被 vX.Y 修正"覆盖的旧条目

---

## 维护规则

- 本文只列**入口与顺序**，不复述被指向文件的内容（避免双地点同步）。
- 路由分层变动时同步更新本文；规则内容变动时不动本文（去对应文件改）。

<!--
  ============================================================
  最终交付前 AI 检查清单：
    [ ] 删掉所有 <!-- AI注: --> 注释（包括本块）
    [ ] 三个路由层按读者实际建的文件删减（没建的不能列！）
    [ ] COLLABORATION 不出现（已合并到 AGENT）
  ============================================================
-->
