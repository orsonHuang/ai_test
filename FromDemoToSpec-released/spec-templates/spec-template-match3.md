# [游戏名称] · 三消系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核
> **游戏模式**：[ ] 回合制（有限步数） / [ ] 即时制（计时器）
>
> 参考 input-example：`input-examples/input-example-match3.md`

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 游戏模式 | [回合制 / 即时制] |
| 棋盘规格 | [行数 × 列数] |
| 元素类型数 | [由 AI 提取] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

---

## 三、棋盘设计

### 3.1 棋盘规格

> 来源：原型代码棋盘初始化函数（`initBoard()` / `createGrid()`）

- 行数：[由 AI 填入]
- 列数：[由 AI 填入]
- 格子形状：[方格 / 六边形 / 其他]

### 3.2 元素类型

> 来源：原型代码 `ELEMENT_TYPES` / `GEM_TYPES` / 同类枚举

| 元素 ID | 名称/颜色 | 权重（生成概率） | 备注 |
|---------|----------|----------------|------|
| [由 AI 填入] | | | |

### 3.3 初始布局生成规则

> 来源：原型代码 `generateBoard()` / `fillBoard()` 函数

- 生成策略：[纯随机 / 权重随机 / 防初始消除]
- 防初始消除：[有/无，有则说明检测方式]

---

## 四、消除规则

### 4.1 基础匹配

> 来源：原型代码 `checkMatches()` / `findMatches()` 函数

- 最小匹配数：[由 AI 填入，通常为 3]
- 匹配方向：[横向 / 纵向 / 斜向]
- 匹配形状：[直线 / L形 / T形 / 其他]

### 4.2 特殊形状消除（如适用）

> 若无特殊形状逻辑，本节填"无"。

| 形状 | 触发条件 | 效果 |
|------|----------|------|
| [由 AI 填入] | | |

### 4.3 连锁/瀑布规则

> 来源：原型代码 `processFalls()` / `checkCascade()` 函数

- 连锁机制：[有/无]
- 连锁计分倍率：[由 AI 填入，无则填"无"]
- 瀑布顺序：[先消除 → 下落 → 补充 → 再检测]

---

## 五、元素物理

### 5.1 重力/下落规则

> 来源：原型代码 `applyGravity()` / `dropElements()` 函数

- 下落方向：[向下 / 向上 / 向左 / 向右]
- 下落动画：[有/无，有则说明帧数]

### 5.2 补充机制

> 来源：原型代码 `refillBoard()` / `spawnElements()` 函数

- 补充来源：[随机生成 / 权重随机 / 预设序列]
- 补充时机：[消除后立即 / 瀑布结束后]

---

## 六、特殊元素（如适用）

> 由特定匹配形状触发生成的道具/增强元素。若无，本节填"无"。

| 元素名 | 触发条件（匹配形状/数量） | 效果 | 激活方式 |
|--------|------------------------|------|----------|
| [由 AI 填入] | | | |

---

## 七、游戏模式

### 7.1 回合制变体（如适用）

> 若游戏模式为回合制，填写本节；否则填"不适用"。

- 初始步数：[由 AI 填入]
- 步数消耗规则：[每次交换 -1 / 每次消除 -1 / 其他]
- 步数奖励机制：[有/无，有则说明触发条件]

### 7.2 即时制变体（如适用）

> 若游戏模式为即时制，填写本节；否则填"不适用"。

- 总时长（秒）：[由 AI 填入]
- 连击时间窗口（ms）：[由 AI 填入，无则填"无"]
- 加时机制：[有/无，有则说明触发条件和加时量]

---

## 八、关卡/目标设计

### 8.1 目标类型

> 来源：原型代码 `checkGoal()` / `LEVEL_DATA` / 关卡目标定义

- 目标类型：[收集 N 个指定元素 / 清除障碍 / 消除特定格子 / 达到分数]
- 具体目标：[由 AI 提取]

### 8.2 计分规则

> 来源：原型代码 `updateScore()` / `calcScore()` 函数

| 操作 | 得分 |
|------|------|
| 基础消除（3 个） | [由 AI 填入] |
| 额外每多 1 个 | [由 AI 填入] |
| 连锁奖励 | [由 AI 填入，无则填"无"] |

---

## 九、系统设计

### 9.1 状态定义

> 来源：原型代码 `state` 对象（排除 `_temp` / `_cache` 前缀字段）

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 9.2 事件清单

> 来源：原型代码 `EventBus.on(...)` 注册点

| 事件名 | 触发时机 | 携带数据 |
|--------|----------|----------|
| [由 AI 填入] | | |

---

## 十、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 10.1 数据边界（棋盘空位/连锁深度/分数溢出）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：连锁深度过深 | 持续瀑布消除 | 上限保护 / 计分封顶 |
| 例：棋盘补充失败 | 元素池耗尽 | 重洗 / 强制重排 |
| | | |

### 10.2 状态/交互边界（交换中再交换/动画中操作）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：动画未完成又触发交换 | 玩家快速滑动 | 加锁防重入 |
| 例：无可消除死局 | 棋盘洗牌后仍无解 | 检测 + 重排 |
| | | |

### 10.3 性能边界（特效叠加/重力计算复杂度）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：消除特效 DOM 堆积 | 高连锁未及时清理 | N 个上限，超出删最早 |
| | | |

### 10.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 十一、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
