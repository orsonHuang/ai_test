# [原型名称] · 技术架构 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。面向"交付给程序员"场景——比通用 spec 多覆盖架构/数据模型/算法伪代码/存储/容错。
> **何时用本模板**：当原型需要交付给程序员理解并重写为正式工程实现，或原型本身工程深度较高（含存储、状态机、性能保护）时。普通设计验证场景仍用 `spec-template-{type}.md`。
> **何时不用**：纯设计验证（玩法是否成立 / 数值是否平衡）→ 选对应玩法类型的通用 spec 模板；本模板偏技术、读者门槛高。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核
> **配套通用 spec**：[与本文件同目录 / 不存在]（推荐两份同时产出：通用 spec 面向策划/数值，本文件面向程序员）

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 原型名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 原型类型 | [战斗 / 即时制 / 经济 / 三消 / ...（与对应通用 spec 一致）] |
| 文件规模 | [HTML 行数 / 主要模块数] |
| 运行时长 | [一局/一次的预期时长，便于性能预算] |
| 关键运行时假设 | [浏览器/单文件/无网络/localStorage 是否启用 等] |

---

## 二、架构概览

### 2.1 模块依赖图

> 来源：原型代码顶层模块划分（按注释 // === XXX === / class / 大段函数簇推断）

```
[由 AI 绘制 ASCII 模块依赖图]
例：
  CONFIG → State → EventBus → GameLoop ──┬→ Logic（玩法/战斗/经济）
                                          └→ Render（DOM/特效/调试面板）
```

### 2.2 主循环类型

| 类型 | 是否采用 | 说明 |
|------|---------|------|
| setInterval（固定 tick） | [ ] | tick 间隔：[ms]，每 tick 步骤见 §三 |
| requestAnimationFrame（帧驱动） | [ ] | 目标帧率：60fps，deltaTime 上限：[ms] |
| 事件驱动（无主循环） | [ ] | 主触发事件列表：[ ] |
| 混合（如战斗 tick + UI raf） | [ ] | 说明分工：[ ] |

### 2.3 关键设计权衡

> 来源：原型代码中"为什么不这么做"的注释 + 显式的取舍点

| 取舍点 | 选定方案 | 备选方案 | 理由 |
|--------|---------|---------|------|
| 例：用 setInterval vs raf | setInterval(300ms) | raf | 战斗节奏感更稳，CPU 占用低 |
| | | | |

---

## 三、数据模型

### 3.1 全局 State 对象

> 来源：原型代码 `state` / `G` 等顶层状态对象

```js
// 由 AI 完整提取（含字段类型 + 一句话注释 + 默认值）
const state = {
  // [字段名]: [类型]  // [一句话含义]
}
```

### 3.2 关键数据结构（CONFIG / DATA）

> 来源：原型代码 `CONFIG` / `*_DATA` / `*_TEMPLATES` 对象

| 名称 | 类型 | 用途 | 关键字段 |
|------|------|------|---------|
| [由 AI 填入] | const / array / map | | |

### 3.3 状态机定义（如有）

> 来源：原型代码状态切换函数（`setState()` / `transitionTo()` / phase 字段）

```
[由 AI 绘制 ASCII 状态机图]
例：
  IDLE → READY → RUNNING → ENDED
                    ↓
                  PAUSED
```

合法转换矩阵：

| from \ to | IDLE | READY | RUNNING | PAUSED | ENDED |
|-----------|------|-------|---------|--------|-------|
| IDLE | - | ✓ | ✗ | ✗ | ✗ |

---

## 四、核心算法（伪代码）

> 来源：原型代码主要业务函数（开箱 / 战斗 tick / 消除判定 / 抽牌 等）
> **必填**：每个核心算法都用伪代码描述（不是粘代码，而是"程序员能照写"的步骤）

### 4.1 [算法名 1]

```
function [算法名](输入参数):
  1. [步骤]
  2. [步骤]
  3. return [输出]

边界处理：
  - [边界条件 1] → [处理]
  - [边界条件 2] → [处理]
```

> 来源：原型代码 L[起] - L[止]

### 4.2 [算法名 2]

（同上格式）

---

## 五、事件与接口

### 5.1 事件清单（EventBus / 自定义事件）

> 来源：原型代码 `EventBus.on('event_name'` / `dispatchEvent`

| 事件名 | 触发时机 | 携带数据 | 监听方 |
|--------|---------|---------|--------|
| [由 AI 填入] | | | |

### 5.2 对外接口（如有）

> 来源：原型代码 `window.*` / `export` / 全局函数

| 接口名 | 用途 | 参数 | 返回 |
|--------|------|------|------|
| [由 AI 填入] | | | |

---

## 六、存储方案（如有）

> 来源：原型代码 `localStorage` / `sessionStorage` / `IndexedDB`

| 存储键 | 数据结构 | 写入时机 | 读取时机 | 失效处理 |
|--------|---------|---------|---------|---------|
| [由 AI 填入] | | | | |

存储版本控制：[ ] 有（key 含版本号） / [ ] 无（迁移策略：）

---

## 七、性能预算

> 来源：原型代码显式 throttle/cap/上限保护 + 试玩观察

| 性能维度 | 上限/预算 | 当前实现 |
|---------|----------|---------|
| 单帧渲染时间 | 16ms（60fps） / 33ms（30fps） | [由 AI 填入] |
| DOM 节点总数 | [N] | [由 AI 填入] |
| 内存峰值 | [估算] | [由 AI 填入] |
| 主循环 deltaTime 上限 | [ms] | [由 AI 填入] |
| 特效/伤害数字上限 | [N] | [由 AI 填入] |

---

## 八、边界条件与容错

> **必填段**。本节是技术 spec 的核心差异——比通用 spec 更深，需逐类列举。

### 8.1 数据边界

| 边界类型 | 触发条件 | 当前处理 | 转开发建议 |
|---------|---------|---------|-----------|
| [由 AI 填入] | | | |

### 8.2 状态机边界（非法转换）

| 非法转换 | 触发条件 | 当前处理 | 转开发建议 |
|---------|---------|---------|-----------|
| [由 AI 填入] | | | |

### 8.3 UI/交互边界

| 边界类型 | 触发条件 | 当前处理 | 转开发建议 |
|---------|---------|---------|-----------|
| [由 AI 填入] | | | |

### 8.4 性能边界

| 边界类型 | 触发条件 | 当前处理 | 转开发建议 |
|---------|---------|---------|-----------|
| [由 AI 填入] | | | |

### 8.5 异常兜底（try-catch / fallback / 降级）

> 来源：原型代码 try-catch + 显式 fallback 逻辑

| 异常类型 | 处理方式 | 转开发建议 |
|---------|---------|-----------|
| [由 AI 填入] | | |

---

## 九、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释 + 通用 spec 同步项

| 位置（函数名 + 行号） | 描述 | 类型 | 严重度 |
|----------------------|------|------|-------|
| [由 AI 填入] | | TODO / FIXME / HACK | P0/P1/P2 |

---

---

## 十、UI 组件树 ★v1.1新增

> 来源：原型代码 `document.createElement` / HTML 结构 / UI 切换逻辑
> **用途**：交付程序员时，树状展示全部 UI 组件及其嵌套关系，避免遗漏子组件重建
> **参考**：06-15 实验中无工作流侧 SPEC 自发包含此段，证明其对交付有用

```
[由 AI 从 HTML 结构提取 UI 组件树]
例：
  #app
  ├── #title-bar（标题栏）
  │   ├── .title-text（游戏标题）
  │   └── .turn-indicator（回合指示器）
  ├── #battle-area（战斗区域）
  │   ├── #enemy-zone（敌方区域）
  │   │   └── .enemy-card × N
  │   ├── #player-zone（己方区域）
  │   │   └── .player-card × N
  │   └── #action-panel（操作面板）
  │       ├── .skill-buttons（技能按钮组）
  │       └── .action-buttons（行动按钮组）
  ├── #log-panel（日志面板）
  └── #result-modal（结果弹窗）
```

---

## 十一、键盘快捷键 ★v1.1新增

> 来源：原型代码 `keydown` / `addEventListener('keydown')` 
> **用途**：记录原型中已实现的键盘操作，交付程序员时确保快捷键不被遗漏

| 快捷键 | 触发行为 | 使用位置/函数 | 备注 |
|--------|---------|-------------|------|
| [由 AI 填入] | | | |
| [由 AI 填入] | | | |

---

## 十二、主题变量 ★v1.1新增

> 来源：原型代码 `:root { --* }` / CSS 变量定义
> **用途**：集中展示原型中使用的颜色/间距/字号等主题变量，方便后续换肤和统一管理

| 变量名 | 默认值 | 用途 | 备注 |
|--------|--------|------|------|
| [由 AI 填入] | | | |
| [由 AI 填入] | | | |

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
