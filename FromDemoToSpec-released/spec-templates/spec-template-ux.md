# [产品名称] · 产品/UX Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 产品/功能名称 | [由 AI 从标题/CONFIG 提取] |
| 目标用户角色 | [由 prototype-input-template UX段提取] |
| 使用场景 | [由 prototype-input-template UX段提取] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、导航结构

> 来源：原型代码 `showPage()` / `navigateTo()` / `switchView()` 等函数

### 2.1 页面清单

| 页面名称 | 对应函数/视图 | 进入条件 | 退出路径 |
|----------|--------------|----------|----------|
| [由 AI 填入] | | | |

### 2.2 页面跳转关系

```
[由 AI 按 showPage/navigateTo 调用关系整理]

页面A → （触发条件） → 页面B
页面B → （触发条件） → 页面C / 页面A
```

---

## 三、交互清单

> 来源：原型代码 `addEventListener` / `onclick` 绑定

| 元素 ID / 描述 | 所在页面 | 交互行为 | 触发事件 |
|----------------|----------|----------|----------|
| [由 AI 填入] | | | |

---

## 四、页面内容规范

> 来源：原型代码各页面渲染函数 `renderXxxPage()` / `drawXxxView()`

### [页面名称1]（由 AI 按页面数量复制本段）

- **展示内容**：[由 AI 提取渲染函数中的 DOM 元素列表]
- **可交互元素**：[由 AI 提取事件绑定元素]
- **数据来源**：[由 AI 提取该页面读取的 state 字段]

---

## 五、状态展示规范

> 来源：原型代码 `showLoading()` / `showError()` / `showEmpty()` 等函数

| 状态 | 触发条件 | UI 表现 |
|------|----------|---------|
| 加载中 | [由 AI 填入，无则填"无"] | |
| 错误 | [由 AI 填入] | |
| 空状态 | [由 AI 填入，无则填"无"] | |
| 成功/完成 | [由 AI 填入] | |

---

## 六、动画/过渡规范

> 来源：原型代码 `requestAnimationFrame` / `transition` / `animate` 相关代码

| 动画场景 | 触发条件 | 参数（时长/缓动） | 备注 |
|----------|----------|-----------------|------|
| [由 AI 填入，无则填"无"] | | | |

---

## 七、全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

---

## 八、事件清单

> 来源：原型代码 `EventBus.on(...)` 注册点

| 事件名 | 触发时机 | 携带数据 |
|--------|----------|----------|
| [由 AI 填入] | | |

---

## 九、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 9.1 数据边界（输入/状态/数据格式）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：空数据/缺字段渲染 | API/数据缺失 | 占位文本 / Empty 状态 |
| | | |

### 9.2 状态/交互边界（导航异常/重复点击/Loading 中操作）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：Loading 中再次点击 | 用户重复触发 | 按钮禁用 / 加锁 |
| 例：跳转到不存在页面 | URL 直接访问 | 404 / 重定向 |
| | | |

### 9.3 性能边界（列表/动画/资源加载上限）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：列表过长卡顿 | 数据 > N | 虚拟滚动 / 分页 |
| | | |

### 9.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 十、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
