# [游戏名称] · 构筑/卡组系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 构筑模式 | [草稿轮抽 / 自由构组 / 限制构组 / 其他] |
| 卡池总卡数 | [由 AI 从 cardPool 数组统计] |
| 每次草稿轮数 | [由 AI 从 PICKS_PER_DRAFT / 相关常量提取] |
| 协同规则数 | [由 AI 从 synergies 数组统计] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、卡池设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 卡牌属性表

> 来源：原型代码 `cards` / `CARD_DATA` 数组

| 卡 ID | 名称 | 费用 | 类型 | 标签 | 基础效果 | 副本数 | 稀有度 |
|-------|------|------|------|------|---------|--------|--------|
| [由 AI 填入] | | | | | | | |

### 2.3 标签体系

> 来源：原型代码 card.tags 字段的枚举值

| 标签 | 含义 | 持有该标签的卡数 |
|------|------|----------------|
| [由 AI 填入] | | |

---

## 三、协同系统

### 3.1 协同规则表

> 来源：原型代码 `synergies` / `SYNERGY_DATA` 数组

| 协同 ID | 标签对 / 卡对 | 触发条件 | 效果描述 | 强度评分 |
|---------|-------------|---------|---------|---------|
| [由 AI 填入] | | | | |

### 3.2 协同检测逻辑

> 来源：原型代码 `checkSynergies()` / `calcSynergyScore()` 函数

```
[由 AI 提取检测算法：何时触发、遍历方式、分值计算]
```

### 3.3 已知路线强度分析

> 来源：原型代码 + input-example 预期路线

| 路线名称 | 核心卡组成 | 预期协同分 | 是否存在压制性优势 |
|---------|-----------|----------|-----------------|
| [由 AI 填入] | | | [ ] 是 / [ ] 否 |

---

## 四、草稿机制

### 4.1 抽牌规则

> 来源：原型代码 `drawCards()` / `generateOffer()` 函数

- 每轮展示数量：[由 AI 填入]
- 抽取策略（随机/权重）：[由 AI 填入]
- 废弃处理（放回/消除）：[由 AI 填入]
- 副本控制：[由 AI 填入]

### 4.2 草稿进程

> 来源：原型代码 `Draft` / `draftState` 对象

```
第 1 轮：[由 AI 描述初始情况]
...
第 N 轮：[牌池耗尽时的处理]
最终：展示卡组 + 协同分析
```

---

## 五、系统设计

### 5.1 状态定义

> 来源：原型代码 `state` 对象

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 5.2 关键函数清单

> 来源：原型代码函数声明

| 函数名 | 职责 | 输入 | 输出 |
|--------|------|------|------|
| [由 AI 填入] | | | |

### 5.3 随机性控制

> 来源：原型代码随机函数调用 + SEED 参数

- 随机源：[由 AI 填入]
- 种子控制：[由 AI 填入]
- 可复现性：[有/无]

---

## 六、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 6.1 数据边界（卡池/费用/数值范围）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：抽牌池耗尽 | 剩余卡数 < 展示数 | 重洗弃牌堆 / 减少展示数 |
| | | |

### 6.2 状态/交互边界（重复选择/非法操作）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：同一轮重复选择 | 玩家快速点击 | 加锁防重入 |
| | | |

### 6.3 性能边界（卡数/协同检测复杂度）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：协同检测 O(n²) 卡顿 | 卡组规模过大 | 分批 / 缓存 |
| | | |

### 6.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 七、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
