# [游戏名称] · 即时制系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核
> **子类型**：[ ] 塔防 / [ ] 自动战斗 / [ ] 其他即时制
>
> 参考 input-example：
> - 塔防示例 → `input-examples/input-example-realtime-towerdefense.md`
> - 自动战斗示例 → `input-examples/input-example-realtime-autobattle.md`

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 即时制子类型 | [塔防 / 自动战斗 / 其他] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 Tick 机制

> 来源：原型代码 `gameTick()` / `setInterval` 主循环

- Tick 间隔（ms）：[由 AI 填入]
- 每 Tick 执行步骤（按顺序）：
  1. [由 AI 按调用顺序填入]
  2.
  3.

---

## 三、实体设计

### 3.1 玩家单位 / 防御塔

> 来源：原型代码 `towers` / `units` / `UNIT_DATA` / `TOWER_DATA`
> 若无，本节填"无"。

| 名称 | 类型 | HP / 耐久 | ATK | 攻击范围 | 攻速 / 冷却 | 费用 |
|------|------|-----------|-----|----------|-------------|------|
| [由 AI 填入] | | | | | | |

### 3.2 敌方单位

> 来源：原型代码 `enemies` / `ENEMY_DATA`

| 名称 | HP | 移速 | 伤害/行为 | 奖励（金/分） |
|------|-----|------|-----------|--------------|
| [由 AI 填入] | | | | |

### 3.3 技能 / 主动能力（如适用）

> 若原型无主动技能系统，本节填"无"。

| 技能名 | 所属单位 | 效果 | 冷却（秒） | 消耗 |
|--------|----------|------|-----------|------|
| [由 AI 填入] | | | | |

---

## 四、生成规则

### 4.1 波次/生成策略

> 来源：原型代码 `spawnWave()` / `enemySpawner` / `WAVE_DATA`

| 波次 | 敌人类型 | 数量 | 生成间隔（ms） | 触发条件 |
|------|----------|------|--------------|----------|
| [由 AI 填入] | | | | |

### 4.2 随机性控制

> 来源：原型代码随机数调用（`Math.random()` / 权重表）

- 随机策略：[纯随机 / 权重随机 / 固定序列]
- 随机种子：[有/无，有则说明控制方式]

---

## 五、路径与地图（如适用）

> 仅塔防类需要。若无路径系统，本节填"无"。

> 来源：原型代码 `path` / `MAP_DATA` / `findPath()`

- 地图规格：[宽×高，格子类型]
- 路径类型：[固定路径 / 动态寻路]
- 路径描述：[由 AI 提取起终点和关键节点]

---

## 六、胜负与关卡条件

> 来源：原型代码 `checkWin()` / `checkGameOver()` / `checkVictory()`

- 胜利条件：[由 AI 提取]
- 失败条件：[由 AI 提取]
- 关卡目标（如有）：[分数 / 存活 / 消灭全部]

---

## 七、系统设计

### 7.1 状态定义

> 来源：原型代码 `state` 对象（排除 `_temp` / `_cache` 前缀字段）

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 7.2 事件清单

> 来源：原型代码 `EventBus.on(...)` 注册点

| 事件名 | 触发时机 | 携带数据 |
|--------|----------|----------|
| [由 AI 填入] | | |

---

## 八、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 8.1 数据边界（HP/费用/生成数量 等数值越界）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：金币溢出 | 累计奖励超出上限 | clamp / 转化为分数 |
| | | |

### 8.2 状态/交互边界（暂停/加速/重开 时的非法状态）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：暂停后再生成敌人 | spawnWave 未检查暂停状态 | 状态检查 + 跳过 |
| 例：切 tab 时间漂移 | deltaTime 累计过大 | dt 上限 300ms |
| | | |

### 8.3 性能边界（实体数/特效数 上限）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：单位数超阈值 | 高波次堆积未清理 | 上限 N，超出拒绝生成 |
| | | |

### 8.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 九、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
