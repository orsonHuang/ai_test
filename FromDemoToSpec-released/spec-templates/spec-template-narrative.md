# [游戏名称] · 叙事/剧情系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 叙事类型 | [线性 / 分支 / DAG / 其他] |
| 节点总数 | [由 AI 从 nodes 数组统计] |
| 结局数量 | [由 AI 统计 isEnding=true 的节点数] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入，无则填"无"] | | | |

---

## 三、故事树结构

### 3.1 节点清单

> 来源：原型代码 `nodes` / `STORY_DATA` 对象

| 节点 ID | 标题 | 是否结局 | 可到达路径数 | 备注 |
|---------|------|---------|------------|------|
| [由 AI 填入] | | | | |

### 3.2 树形结构图

> 来源：原型代码节点 `nextNodeId` 指向关系

```
[由 AI 按 nextNodeId 关系绘制 ASCII 树 / DAG]
```

### 3.3 结局分类

> 来源：原型代码 `isEnding = true` 的节点 + `endingType` / `endingId`

| 结局 ID | 结局名称 | 触发条件（关键 flags / 数值门槛） |
|---------|---------|--------------------------------|
| [由 AI 填入] | | |

---

## 四、变量设计

### 4.1 数值型变量

> 来源：原型代码 `PlayerState` / `gameState` 对象（排除 flags 字段）

| 变量名 | 类型 | 初始值 | 上限 | 下限 | 说明 |
|--------|------|--------|------|------|------|
| [由 AI 填入] | number | | | | |

### 4.2 旗帜型变量（flags）

> 来源：原型代码 `PlayerState.flags` / `state.flags`

| Flag 名 | 类型 | 默认值 | 设置时机 | 用途 |
|---------|------|--------|---------|------|
| [由 AI 填入] | boolean/string | | | |

---

## 五、选项与效果规则

### 5.1 选项结构

> 来源：原型代码 `choices` / `options` 数组

每个选项包含：
- `label`：显示文字
- `resultText`：选择后的结果描述
- `nextNodeId`：跳转目标
- `effects[]`：对 PlayerState 的修改
- `condition`（可选）：显示条件

### 5.2 效果类型清单

> 来源：原型代码 `applyEffect()` / `executeChoice()` 函数

| 效果类型 | 操作说明 | 示例 |
|---------|---------|------|
| stat_add | 数值型变量加减 | hp += -10 |
| flag_set | 旗帜型变量赋值 | flags.helped_village = true |
| [由 AI 补充] | | |

### 5.3 条件选项规则

> 来源：原型代码 `isChoiceVisible()` / `filterChoices()` 函数

```
[由 AI 提取条件判断逻辑]
```

---

## 六、系统设计

### 6.1 状态定义

> 来源：原型代码 `state` 对象

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 6.2 关键函数清单

> 来源：原型代码函数声明

| 函数名 | 职责 | 输入 | 输出 |
|--------|------|------|------|
| [由 AI 填入] | | | |

### 6.3 情感曲线（节点压力序列）

> 来源：原型代码节点顺序 + inputTemplate 情感曲线字段

```
[由 AI 按节点顺序标注：平静 / 低压 / 中压 / 高压 / 释放]
```

---

## 七、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 7.1 数据边界（数值/旗帜/计数范围）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：好感度溢出上限 | applyEffect 累加超出最大值 | clamp 到上限 |
| | | |

### 7.2 状态/交互边界（非法跳转 / 异常选择）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：跳到不存在的节点 | nextNodeId 无对应 node | 报错 + 回退到上一节点 |
| 例：条件选项不显示 | filterChoices 全部过滤 | 兜底显示"继续"或自动推进 |
| | | |

### 7.3 性能边界（节点数/选项数上限）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：单节点选项过多 | choices.length > N | 折叠/分页 |
| | | |

### 7.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 八、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
