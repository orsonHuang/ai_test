# [产品名称] · 表单/流程 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 产品名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 流程步骤数 | [由 AI 从 steps 数组统计] |
| 字段总数 | [由 AI 统计所有步骤的 fields 总数] |
| 条件字段数 | [由 AI 统计 conditionalOn 非空的字段数] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、字段设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 完整字段清单

> 来源：原型代码 `FIELD_CONFIG` / `steps[].fields` 数组

| 字段 ID | 所属步骤 | 标签 | 类型 | 必填 | 校验规则 | 条件显示 |
|---------|---------|------|------|------|---------|---------|
| [由 AI 填入] | | | | | | |

### 2.3 条件字段依赖关系

> 来源：原型代码 `conditionalOn` / `isFieldVisible()` 函数

```
[由 AI 绘制条件依赖图]
例：
  is_private = true  →  显示 password 字段
  game_mode = "竞技"  →  显示 ranked_match 字段；隐藏 enable_spectate 字段
```

---

## 三、校验规则

### 3.1 字段级校验

> 来源：原型代码 `validateField()` / `VALIDATION_RULES` 对象

| 字段 ID | 校验时机 | 规则描述 | 错误提示文案 |
|---------|---------|---------|------------|
| [由 AI 填入] | 失焦/提交 | | |

### 3.2 跨字段校验

> 来源：原型代码 `validateStep()` / `validateForm()` 函数中的跨字段逻辑

| 校验 ID | 涉及字段 | 规则描述 | 触发时机 | 错误展示位置 |
|---------|---------|---------|---------|------------|
| [由 AI 填入] | | | | |

### 3.3 步骤解锁条件

> 来源：原型代码 `canProceed()` / 下一步按钮禁用逻辑

| 步骤 | 解锁条件（哪些字段必须通过才能进入下一步） |
|------|----------------------------------------|
| [由 AI 填入] | |

---

## 四、流程设计

### 4.1 状态定义

> 来源：原型代码 `state` 对象

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 4.2 步骤切换规则

> 来源：原型代码 `goNext()` / `goBack()` / `goToStep()` 函数

- 前进：[由 AI 填入]
- 后退：[由 AI 填入，数据是否保留]
- 跳步：[允许/禁止]

### 4.3 数据持久化

> 来源：原型代码 `FormState` 管理逻辑

- 步骤切换时数据：[保留 / 部分清空 / 全清]
- 提交失败后数据：[保留 / 清空]
- 刷新页面后数据：[由 localStorage / sessionStorage / 不保留]

---

## 五、提交与结果

### 5.1 提交前置检查

> 来源：原型代码 `onSubmit()` 函数

```
[由 AI 提取提交前的校验顺序]
```

### 5.2 提交状态展示

> 来源：原型代码 `submitting` 状态处理

- Loading 时：[由 AI 填入]
- 成功时：[由 AI 填入]
- 失败时：[由 AI 填入]

---

## 六、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 6.1 数据边界（字段值/长度/格式）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：超长输入 | 字符数 > maxLength | 截断 / 阻止输入 / 错误提示 |
| 例：粘贴含非法字符 | 非数字字段粘贴文本 | 过滤 / 阻止 |
| | | |

### 6.2 状态/交互边界（前进后退/中途退出/网络异常）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：未保存就后退 | 浏览器后退键 | 提示挽留 / 自动暂存 |
| 例：提交中网络断开 | submit 进行中失联 | 重试 / 错误提示 + 保留数据 |
| | | |

### 6.3 性能边界（条件依赖链 / 联动校验复杂度）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：联动字段过多卡顿 | 触发链 > N | 节流 / 异步 |
| | | |

### 6.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 七、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
