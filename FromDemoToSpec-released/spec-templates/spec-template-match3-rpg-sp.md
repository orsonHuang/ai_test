# [游戏名称] · 即时制三消+RPG 复合系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核
>
> ⚠ **定制模板（-sp），不通用**：本模板专为"即时制三消驱动 RPG 战斗"设计，不适用于纯三消或纯 RPG 原型。
> 通用三消请使用 `spec-template-match3.md`，通用回合制战斗请使用 `spec-template-combat.md`。
>
> 参考 input-example：`input-examples/input-example-match3-rpg-sp.md`

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 游戏模式 | 即时制（计时器驱动）× RPG 战斗层 |
| 棋盘规格 | [行数 × 列数] |
| 元素类型数 | [由 AI 提取] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 【三消层】

---

## 二、棋盘设计

### 2.1 棋盘规格

> 来源：原型代码 `initBoard()` / `createGrid()`

- 行数：[由 AI 填入]
- 列数：[由 AI 填入]
- 格子形状：[方格 / 六边形 / 其他]

### 2.2 元素类型

> 来源：原型代码 `ELEMENT_TYPES` / `GEM_TYPES` / 同类枚举

| 元素 ID | 名称/颜色 | 对应 RPG 语义 | 生成权重 |
|---------|----------|--------------|---------|
| [由 AI 填入] | | [如：红色→物理攻击，蓝色→技能] | |

> **注意**：元素的 RPG 语义（"消除红色格 → 造成物理伤害"）在§六中定义，此处只记录元素本身属性。

### 2.3 初始布局生成规则

> 来源：原型代码 `generateBoard()` / `fillBoard()`

- 生成策略：[纯随机 / 权重随机 / 防初始消除]
- 防初始消除：[有/无]

---

## 三、消除规则

### 3.1 基础匹配

> 来源：原型代码 `checkMatches()` / `findMatches()`

- 最小匹配数：[由 AI 填入]
- 匹配方向：[横向 / 纵向 / 斜向]

### 3.2 连锁/瀑布规则

> 来源：原型代码 `processFalls()` / `checkCascade()`

- 连锁机制：[有/无]
- 瀑布顺序：[消除 → 下落 → 补充 → 再检测]

### 3.3 特殊元素（如适用）

> 若无特殊形状/道具逻辑，本节填"无"。

| 元素名 | 触发条件 | 效果 | RPG 效果映射 |
|--------|----------|------|------------|
| [由 AI 填入] | | | |

---

## 四、元素物理

### 4.1 重力/下落规则

- 下落方向：[向下 / 向上 / 其他]

### 4.2 补充机制

- 补充来源：[随机 / 权重]
- 补充时机：[瀑布结束后]

---

## 五、即时制计时器

> 来源：原型代码 `timer` / `countdown` / `gameTick()` / `setInterval`

- 单局时长（秒）：[由 AI 填入]
- 连击时间窗口（ms）：[由 AI 填入，无则填"无"]
- 加时机制：[有/无，有则说明触发条件和加时量]
- Tick 间隔（ms）：[由 AI 填入]

---

## 【RPG 层】

---

## 六、三消→战斗映射规则

> 核心规则：三消操作结果如何驱动 RPG 数值。
> 来源：原型代码中连接棋盘消除结果与战斗逻辑的映射函数（`applyMatchEffect()` / `onMatchResolved()` 等）

| 消除类型 | 触发条件 | 战斗效果 |
|----------|----------|----------|
| [如：消除 3 个红色] | [匹配颜色+数量] | [造成 ATK×1 物理伤害] |
| [如：消除 5 个蓝色] | | [触发技能] |
| [由 AI 填入] | | |

---

## 七、角色设计

### 7.1 玩家角色

> 来源：原型代码 `player` / `PLAYER_DATA` / `heroConfig`

| 属性 | 值 | 说明 |
|------|-----|------|
| HP | [由 AI 填入] | |
| ATK | [由 AI 填入] | |
| DEF | [由 AI 填入，无则填"无"] | |
| 其他属性 | [由 AI 填入] | |

### 7.2 技能表（如适用）

> 若无主动技能系统，本节填"无"。

| 技能名 | 触发条件（消除动作） | 效果 | 冷却（秒 / 连击数） |
|--------|-------------------|------|------------------|
| [由 AI 填入] | | | |

---

## 八、敌人设计

> 来源：原型代码 `enemies` / `ENEMY_DATA`

### 8.1 敌人属性表

| 名称 | HP | ATK | 攻击间隔（秒） | 特殊行为 |
|------|-----|-----|--------------|---------|
| [由 AI 填入] | | | | |

### 8.2 敌人行为

> 来源：原型代码 `enemyAction()` / `aiAttack()` / `enemyTick()`

- 攻击模式：[定时攻击 / 条件触发 / 随机]
- 目标选择：[玩家 HP 最低 / 随机 / 固定]

---

## 九、战斗胜负条件

> 来源：原型代码 `checkWin()` / `checkGameOver()`

- 胜利条件：[由 AI 提取]
- 失败条件：[由 AI 提取]
- 关卡奖励：[由 AI 填入，无则填"无"]

---

## 【系统层】

---

## 十、数值设计

### 10.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

---

## 十一、系统设计

### 11.1 状态定义

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 11.2 事件清单

| 事件名 | 触发时机 | 携带数据 |
|--------|----------|----------|
| [由 AI 填入] | | |

---

## 十二、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 12.1 数据边界（HP/伤害/连锁/分数 越界）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| | | |

### 12.2 状态/交互边界（消除中 / 战斗动画中操作）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：消除动画未完成又触发交换 | 玩家快速滑动 | 加锁防重入 |
| 例：战斗结束后又触发匹配 | 双重触发 | 状态检查 + 提前 return |
| | | |

### 12.3 性能边界（特效/伤害数字/单位上限）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| | | |

### 12.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 十三、已知问题

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
