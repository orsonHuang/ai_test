# [游戏名称] · 经济/资源流系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 资源类型数 | [由 AI 从 resources 数组统计] |
| 建筑/产出源数 | [由 AI 从 buildings 数组统计] |
| 验证窗口时长 | [由 prototype-input-template A 段提取] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 资源定义表

> 来源：原型代码 `resources` / `RESOURCE_DATA` 对象

| 资源 ID | 名称 | 初始量 | 上限(cap) | 产出来源 | 主要消耗去向 |
|---------|------|--------|-----------|---------|------------|
| [由 AI 填入] | | | | | |

### 2.3 建筑/产出源定义表

> 来源：原型代码 `buildings` / `BUILDING_DATA` 对象

| 建筑 ID | 名称 | 产出资源 | 基础产出量/tick | 升级成本公式 | 解锁条件 |
|---------|------|---------|--------------|------------|---------|
| [由 AI 填入] | | | | | |

### 2.4 升级成本公式

> 来源：原型代码 `calcUpgradeCost()` / `getUpgradeCost()` 函数

```
[由 AI 提取公式]
```

---

## 三、资源流图

### 3.1 产出-消耗链

> 来源：原型代码 building.outputResource + building.cost 字段关系

```
[由 AI 按资源流向绘制 ASCII 流图]
例：木材 ←（伐木场）     石料 ←（石矿）
         ↓                     ↓
         建造农场、石矿      升级建筑
```

### 3.2 解锁链路

> 来源：原型代码 `building.unlockCondition` 字段

```
[由 AI 按解锁前置关系绘制]
```

---

## 四、经济规则

### 4.1 Tick 机制

> 来源：原型代码 `gameTick()` / `setInterval` 调用

- Tick 间隔：[由 AI 填入] 秒
- 每 tick 执行顺序：
  ```
  1. [由 AI 按函数调用顺序填入]
  2.
  3.
  ```

### 4.2 溢出与上限规则

> 来源：原型代码 `collectResource()` / `addResource()` 函数

```
[由 AI 提取溢出处理逻辑]
```

### 4.3 死锁检测

> 来源：原型代码 `checkDeadlock()` / 相关注释

- 死锁触发条件：[由 AI 填入]
- 处理方式：[由 AI 填入]

---

## 五、里程碑/解锁事件

> 来源：原型代码 `checkUnlock()` / `onMilestone()` 函数

| 里程碑 ID | 触发条件 | 效果 | 提示文案 |
|---------|---------|------|---------|
| [由 AI 填入] | | | |

---

## 六、系统设计

### 6.1 状态定义

> 来源：原型代码 `state` 对象（排除 `_temp` / `_cache` 前缀字段）

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 6.2 关键事件流

> 来源：原型代码 `logEvent()` / `console.log()` 调用推断

```
[由 AI 按日志顺序整理：tick → 产出 → 校验上限 → 触发解锁检查 → UI 刷新]
```

---

## 七、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 7.1 数据边界（数值范围 / 上下限 / 溢出）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：资源溢出 cap | addResource 结果 > cap | clamp 到 cap，超出量丢弃 |
| | | |

### 7.2 状态/交互边界（非法状态切换 / 异常操作）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：tick 暂停后再启动 | 快速点击暂停/继续 | 加锁防重入 |
| | | |

### 7.3 性能边界（资源/数量上限 / 帧率保护）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：DOM 节点堆积 | 高频特效未及时清理 | N 个上限，超出删最早 |
| | | |

### 7.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 八、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
