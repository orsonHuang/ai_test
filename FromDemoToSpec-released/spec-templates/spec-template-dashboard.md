# [产品名称] · 数据可视化/仪表板 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 产品名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 目标受众 | [由 prototype-input-template 选填区提取] |
| 指标总数 | [由 AI 从 metrics / cards 数组统计] |
| 预设数据集数 | [由 AI 从 datasets 数组统计] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 指标定义表

> 来源：原型代码 `metrics` / `METRIC_DATA` / `cards` 数组

| 指标 ID | 名称 | 单位 | 正常值示例 | 异常阈值（±%） | 默认图表类型 |
|---------|------|------|----------|--------------|------------|
| [由 AI 填入] | | | | | |

### 2.3 预设数据集

> 来源：原型代码 `datasets` / `DATASET_DATA` 对象

| 数据集 ID | 名称 | 描述 | 触发异常的指标 |
|---------|------|------|-------------|
| [由 AI 填入] | | | |

---

## 三、异常检测规则

### 3.1 异常判定逻辑

> 来源：原型代码 `isAnomaly()` / `checkAnomaly()` 函数

```
[由 AI 提取判定表达式]
```

### 3.2 多异常优先级

> 来源：原型代码 `sortCards()` / `rankAnomalies()` 或相关配置

- 最大同时高亮数：[由 AI 从 MAX_HIGHLIGHTED_CARDS 提取]
- 排序规则：[由 AI 填入]

---

## 四、布局设计

### 4.1 卡片布局

> 来源：原型代码 `layout` / `LAYOUT_CONFIG` 数组

| 卡片 ID | 位置（行/列） | 占格数 | 视觉权重 | 说明 |
|---------|------------|--------|---------|------|
| [由 AI 填入] | | | | |

### 4.2 交互状态

> 来源：原型代码状态机

| 状态 | 触发条件 | UI 变化 |
|------|---------|---------|
| viewing | 默认 | 正常展示所有卡片 |
| detail | 点击卡片 | 展开图表浮层 |
| switching | 切换粒度/数据集 | 过渡动画 |
| [由 AI 补充] | | |

---

## 五、图表规格

### 5.1 图表类型与用途

> 来源：原型代码 `renderChart()` / `drawChart()` 函数

| 图表类型 | 用于哪些指标 | 数据格式要求 |
|---------|------------|------------|
| [由 AI 填入] | | |

### 5.2 时间粒度

> 来源：原型代码 `timeGranularity` / `granularity` 枚举

| 粒度 | 数据点数 | 日期格式 |
|------|---------|---------|
| 日 | [由 AI 填入] | |
| 周 | | |
| 月 | | |

---

## 六、系统设计

### 6.1 状态定义

> 来源：原型代码 `state` 对象（排除 `_temp` / `_cache` 前缀字段）

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 6.2 关键函数清单

> 来源：原型代码函数声明

| 函数名 | 职责 | 输入 | 输出 |
|--------|------|------|------|
| [由 AI 填入] | | | |

---

## 七、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 7.1 数据边界（指标值/时间范围/缺失数据）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：指标值为 null / NaN | 数据源缺失 | 显示"-" / 不参与排序 |
| 例：异常阈值除零 | 分母为 0 | 兜底返回 0 / 不报警 |
| | | |

### 7.2 状态/交互边界（切换粒度/数据集时的异常）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：粒度切换中再切换 | 异步加载竞态 | 取消上次请求 / loading 状态 |
| | | |

### 7.3 性能边界（数据量/渲染图表数）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：图表数据点过多 | datapoints > N | 降采样 / 分批渲染 |
| | | |

### 7.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 八、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
