# [游戏名称] · 战斗系统 Spec v0.1

> **模板用途**：由 AI 从 HTML 原型自动提取并填入。人工复核后升为 v1.0。
> **来源原型**：`[原型文件路径]`（由 AI 填入）
> **提取时间**：[由 AI 填入]
> **人工复核**：[ ] 未复核 / [ ] 已复核

---

## 一、概述

| 字段 | 内容 |
|------|------|
| 游戏名称 | [由 AI 从 CONFIG / 文档标题提取] |
| 对战模式 | [PvP / PvE / 自走棋 / 其他] |
| 战场规格 | [宽×高，格子类型] |
| 核心验证目标 | [由 prototype-input-template A 段提取] |

---

## 二、数值设计

### 2.1 全局参数表

> 来源：原型代码 `CONFIG` 对象

| 参数名 | 默认值 | 可调范围 | 说明 |
|--------|--------|----------|------|
| [由 AI 填入] | | | |

### 2.2 伤害公式

> 来源：原型代码 `calcDamage()` 函数

```
[由 AI 提取函数体公式]
```

边界：
- 最小伤害：[由 AI 填入]
- 最大伤害：[由 AI 填入，若有 cap]
- 暴击机制：[有/无，有则补充说明]

---

## 三、单位设计

### 3.1 属性表

> 来源：原型代码 `units` / `UNIT_DATA` / `unitTemplates`

| 名称 | 类型 | HP | ATK | DEF | 移速/范围 | 阵营 |
|------|------|-----|-----|-----|-----------|------|
| [由 AI 填入] | | | | | | |

### 3.2 技能表（如适用）

> 来源：原型代码 `skills` 对象 / `useAbility()` / `castSkill()`
> 若原型无技能系统（无上述函数/对象），本节填"无"，不强制保留空表。

| 技能名 | 所属单位 | 效果描述 | 冷却（回合） | 消耗 |
|--------|----------|----------|------------|------|
| [由 AI 填入，无则填"无"] | | | | |

---

## 四、战斗规则

### 4.1 回合结构

> 来源：原型代码 `nextTurn()` / `endTurn()` 函数

每回合依次执行：

```
1. [由 AI 按函数调用顺序填入]
2.
3.
```

### 4.2 移动规则

> 来源：原型代码 `moveUnit()` / `getReachableCells()`

- 移动范围计算方式：[由 AI 填入]
- 障碍物处理：[由 AI 填入]
- 穿透/占格规则：[由 AI 填入]

### 4.3 胜负条件

> 来源：原型代码 `checkWin()` / `checkGameOver()`

- 胜利条件：[由 AI 提取 return 表达式]
- 失败条件：[由 AI 填入]
- 平局处理：[有/无，有则补充说明]

### 4.4 AI 行为说明

> 来源：原型代码 `aiMove()` / `aiDecide()`

- 目标选择优先级：[由 AI 提取决策优先级逻辑]
- 行为分支：[由 AI 提取条件分支]

---

## 五、系统设计

### 5.1 状态定义

> 来源：原型代码 `state` 对象（排除 `_temp` / `_cache` 前缀字段）

| 字段名 | 类型 | 含义 |
|--------|------|------|
| [由 AI 填入] | | |

### 5.2 事件清单

> 来源：原型代码 `EventBus.on(...)` 注册点

| 事件名 | 触发时机 | 携带数据 |
|--------|----------|----------|
| [由 AI 填入] | | |

### 5.3 关键事件流

> 来源：原型代码 `logEvent()` / `console.log()` 调用推断

```
[由 AI 按日志顺序整理]
```

---

## 六、边界条件与容错

> **必填段**。从原型代码 + 试玩观察中提取，缺项填"无"，不留空白。
> **目的**：把"代码隐含的防御逻辑 / 试玩中暴露的边角异常"显性化，避免转开发后被当作"未定义行为"重新发现。

### 6.1 数据边界（HP/伤害/移动范围 等数值越界）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：HP 减为负 | calcDamage 结果 > 当前 HP | clamp 到 0，触发死亡事件 |
| 例：暴击 / 闪避溢出 | 词条叠加超出上限 | clamp 到上限 |
| | | |

### 6.2 状态/交互边界（回合/技能/动画的非法切换）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：胜负判定后又触发 tick | 双重触发 | 状态检查 + 提前 return |
| 例：动画未完成切回合 | 玩家快速操作 | 加锁防重入 |
| | | |

### 6.3 性能边界（单位数/技能特效数 上限）

| 边界类型 | 触发条件 | 当前处理 |
|---------|---------|---------|
| 例：伤害数字 DOM 堆积 | 高频触发未及时清理 | N 个上限，超出删最早 |
| | | |

### 6.4 异常兜底（try-catch / fallback）

> 来源：原型代码 try-catch 块 + 显式 fallback 逻辑

| 异常类型 | 处理方式 |
|---------|---------|
| [由 AI 填入] | |

---

## 七、已知问题

> 来源：原型代码 `// TODO` / `// FIXME` / `// HACK` 注释

| 位置（函数名 + 行号） | 描述 | 类型 |
|----------------------|------|------|
| [由 AI 填入] | | TODO / FIXME / HACK |

---

---

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | [提取日期] | AI 自动提取初稿 |
| v1.0 | [复核日期] | 人工复核完成 |
